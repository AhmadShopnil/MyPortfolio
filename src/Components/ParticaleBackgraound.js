import React from 'react';
import Particles from "react-particles";
import particalConfig from './Particales_config';


const ParticaleBackgraound = () => {
    return (
        <Particles params={particalConfig}></Particles>
    );
};

export default ParticaleBackgraound;